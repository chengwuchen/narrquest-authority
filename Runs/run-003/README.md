### Run 003

This run tests response behavior when definitions are fixed and cannot be modified.

The model commits to a single principle and maintains it across all scenarios,
including those that introduce direct conflict.

Unlike previous runs, the model explicitly acknowledges contradiction
and resolves it by applying the rule without adjustment.

This pattern shows that when semantic flexibility is removed,
consistency is maintained through strict rule execution.
